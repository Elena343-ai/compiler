package miden:counter-contract@0.1.0;

use miden:base/core-types@1.0.0;

interface counter {
    use core-types.{felt};

    get-count: func() -> felt;
    increment-count: func() -> felt;
}

world counter-world {
    include miden:core-import/all@1.0.0;

    export counter;
}
